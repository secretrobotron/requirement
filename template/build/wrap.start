/*
{project}
Copyright (c) 2011 {authors}
All rights reserved.

{license}
*/

(function (root) {

  // Bail if {project} is already loaded in the page.
  if ( this.{loader} ) {
    return;
  }

  // Hold on to any global define, to allow registering this
  // library as an AMD loader if it is on the page.
  var oldDefine = this.define;